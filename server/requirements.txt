fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic-settings==2.14.2
# Download stage (T-004). yt-dlp releases often, and YouTube changes break older
# pins fast — bump this deliberately (re-verify a real download when you do).
yt-dlp==2026.8.19

# beets is the import framework (ADR-005): candidate generation (chroma), MusicBrainz
# hydration, and organize/dedup — NOT the tagger. T-224 (ADR-033) retired beets from
# tag-writing (mutagen writes now), so only the `chroma` extra remains (→ pyacoustid);
# `musicbrainz` needs no extra (self-contained HTTP client in 2.12, no musicbrainzngs).
# Dropped with the tag plugins: pylast (lastgenre), beautifulsoup4 + langdetect
# (fetchart/lyrics), and Pillow's free ride via embedart (now pinned directly below).
# Pinned to 2.12 on purpose: `importer` / `autotag` are not beets' stable API, so an
# upgrade means re-testing the review-queue seam (architecture.md). `fpcalc` (Chromaprint)
# is a system binary chroma shells out to — NOT a pip dep; see server/README.md.
beets[chroma]==2.12.0

# Pillow: image validation + thumbnail centre-crop-to-square in app/artwork.py. Until
# T-224 it arrived transitively via beets' `embedart` extra; with that plugin retired it
# is a direct dependency and must be pinned explicitly (matching the prior resolution).
Pillow==12.3.0

# Used directly by app/artwork.py (T-007 Door B) to fetch cover art from the Cover Art
# Archive / iTunes. Until T-224 beets' fetchart/lyrics extras pulled it transitively; with
# those retired, this explicit pin is what keeps requests present.
requests==2.34.2

# The R1.5 reconcile call (app/reconcile.py, T-204): one structured-output tool-use
# request per track to `claude-haiku-4-5`, temperature 0. Imported lazily and only
# when ANTHROPIC_APIKEY is set (T-200) — absent, the pipeline degrades to the R1
# fingerprint-only gate and never touches this.
anthropic==0.121.0
