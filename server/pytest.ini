[pytest]
# tests/ has no __init__.py, so pytest's default import mode would put server/tests
# on sys.path instead of server/ — `from app.download import ...` then fails. Anchor
# the import root at server/ so `pytest` works as documented, not just `python -m pytest`.
pythonpath = .
