# T-218 — Profile the identify block: real per-sub-step timing before optimizing

> **Backlog (untriaged).** Filed 2026-08-23. A **measure-first gate** for the speed work: before pulling
> any identify-speed lever (T-215 / T-208 / a possible LLM-latency ticket), get the *real* per-sub-step
> split so we cut the actual bottleneck, not the assumed one. Continues the T-21x speed series.

- **Status:** todo — untriaged. **Gates** T-215 vs T-208 (and a possible new LLM ticket) — decides which.
- **What we already measured (2026-08-23, isolated `/verify` run, Nines "Franklin" `D5QjfJao9FQ`, parks):**
  - Total to review: **28.0s** — download **~4.2s**, transcode→MP3 320 **~4.4s** (ADR-002, not a lever),
    **identify ~19.4s** (the fat part).
  - Real library confirmed untouched; ran on the isolated :8138 instance per the verify skill.
- **The gap.** The pipeline logs **no per-sub-step timing** inside identify, so that ~19.4s is an opaque
  block of four stacked steps: **AcoustID** (fingerprint + lookup), **Shazam** (the ≤8s tail), the
  **MusicBrainz 5-candidate search** (~4–5s, serialized behind MB's 1/sec courtesy limit), and a full
  **LLM reconcile** call (the 2-of-3 vote). We cannot see which slice is fattest — so optimizing now is
  guessing.
- **The shape of the work.** Add temporary timers / structured log lines around the four identify
  sub-steps (in the **isolated** instance, nothing committed to the engine), re-run against (a) the
  parking fixture and (b) a clean-landing track, and record the real split. Then name the fattest slice
  as the optimization target.
- **What it decides.**
  - If **Shazam's tail** dominates → **T-215** (run Shazam *during* the MB search) is the safe, non-engine,
    buildable-now win.
  - If the **MB fan-out** dominates → **T-208** (collapse the 5-candidate hydration) is the lever, but it
    touches the engine and is gated to R2.5.
  - If the **LLM call** is the surprise fat one → file a new ticket; trim carefully, it's the accuracy vote.
- **Constraint to respect:** the ADRs forbid parallelizing *across tracks* (batch stays sequential);
  overlapping sub-steps *within one track* (what T-215 does) is allowed.
- **Related:** **T-215** · **T-208** · **T-214** (perceived-speed narration) · **T-216** (built) ·
  **T-210** (shared MB rate limiter). Evidence: this session's verify run.
- **Done when:** a recorded per-sub-step timing breakdown of the identify block (AcoustID / Shazam / MB /
  LLM) exists from an isolated run, and the fattest slice is named as the target — turning the
  T-215-vs-T-208 choice from assumption into evidence.
