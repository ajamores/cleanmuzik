# T-040 — keep_untagged resolve fails and re-parks

**Status:** **RESOLVED (2026-08-05) — already fixed by `8ba2c2f`, never re-tested.**
**Found:** 2026-08-02 (T-102 browser verify)

## Resolution (2026-08-05)

Not a distinct bug — the same failure as the `8ba2c2f` dup-stage defusal (2026-08-03,
ADR-009 amendment), which landed the *day after* this was filed and was never re-tested
against it. The symptom here ("the resolved song did not land — nothing to show") is the
SKIP signature that fix describes: beets' import duplicate stage false-matched every import
against **keep-untagged items (empty `mb_trackid`)**, so a keep-untagged resolve collided
with a *previously-landed* keep-untagged item and was skipped instead of filed.

**Re-verified end-to-end, isolated** (temp `DB_PATH` + `LIBRARY_DIRECTORY`, real library
untouched): landed one keep-untagged item first (`Nines/Franklin.mp3`, `mb_trackid None` —
the poison precondition), then parked the exact T-040 track (`1bIUqEaCMP4`, the 15-min rip)
and keep-untagged-resolved it → **landed `Dave East/Moonwalking.mp3`, 320 kbps, owner tags,
empty MBID; the review left the queue and did not re-park.** The scenario that failed on
2026-08-02 now succeeds. All three no-candidate exits (reject / re-search / keep-untagged)
work — §8 item 4 fully satisfied, not just "at least reject."

## Problem

A cold-loaded weak-match review resolved with "Keep with my tags" (keep_untagged,
ADR-020 exit 2) fails and re-parks with: "That match couldn't be applied — the resolved
song did not land — nothing to show."

The re-park recovery works (the row reappears in the inbox without a reload —
`watchColdResolve` confirmed), but the underlying resolve should not be failing. Needs
investigation: could be a staging-file issue (the test track was a 15-minute fan-uploaded
mixtape rip), a code-path bug in the keep_untagged worker flow, or a file-system
permission issue.

## Test track

YouTube: `1bIUqEaCMP4` — "Moonwalking - Dave East - Throwback Thursday Studio Session"
(~15 min fan-uploaded mixtape compilation). Parked as a weak match with no AcoustID
fingerprint.

## Done when

A keep_untagged resolve on a parked review lands the file in the library directory with
owner-supplied tags. The review leaves the queue and does not re-park.
